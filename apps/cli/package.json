{
  "name": "@pws/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "pws": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pws/sdk": "workspace:*"
  }
}
